import { TextReductionPipe } from './text-reduction.pipe';

describe('TextReductionPipe', () => {
  it('create an instance', () => {
    const pipe = new TextReductionPipe();
    expect(pipe).toBeTruthy();
  });
});
